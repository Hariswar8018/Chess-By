import 'package:flutter/material.dart';

class Input_User extends StatelessWidget {
  const Input_User({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
